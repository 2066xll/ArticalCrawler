# 实现上一章爬取功能

## 1. 代码修改计划

### 1.1 article_crawler.py

#### a. 增强parse_article函数
- 添加上一章链接提取逻辑，类似于下一章提取
- 使用类似的选择器列表，但针对上一章链接
- 添加上一章文本选择器（如"上一章"、"上节"等）
- 在返回结果中添加`prev_chapter_url`字段

#### b. 修改命令行参数
- 添加`--prev`或`-p`选项，用于指定要爬取的上一章数量
- 添加方向选项，支持向前、向后或双向爬取

#### c. 修改main函数
- 添加上一章爬取逻辑
- 支持从当前章节向前爬取
- 实现章节计数逻辑，确保符合用户输入

### 1.2 app.py

#### a. 添加上一章支持
- 在API中添加上一章爬取参数
- 修改任务执行逻辑，支持上一章爬取

#### b. 前端支持
- 在index.html中添加上一章爬取选项
- 更新表单和任务提交逻辑

### 1.3 模板文件

#### a. index.html
- 添加上一章数量输入框
- 更新任务状态显示
- 更新结果展示

## 2. 实现步骤

1. **修改parse_article函数**：添加上一章链接提取
2. **更新命令行参数**：添加上一章选项
3. **修改main函数**：实现上一章爬取逻辑
4. **更新app.py**：添加API支持
5. **更新前端模板**：添加上一章选项
6. **测试功能**：验证上一章爬取是否正常工作

## 3. 关键技术点

- 上一章链接提取：使用类似下一章的选择器策略
- 章节计数：确保向前爬取时计数正确
- URL处理：正确处理相对链接和绝对链接
- 章节合并：确保同一章节的不同部分正确合并
- 前端交互：提供直观的上一章爬取选项

## 4. 测试计划

1. **基础测试**：爬取指定数量的上一章
2. **混合测试**：同时爬取上一章和下一章
3. **边界测试**：从第一章开始爬取上一章
4. **大规模测试**：爬取大量上一章
5. **Web界面测试**：验证前端功能是否正常

这个计划将实现完整的上一章爬取功能，包括命令行和Web界面支持。